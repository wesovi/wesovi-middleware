vagrant up development
vagrant provision development --provision-with=docker
vagrant ssh development

-->
docker ps
docker ps -a
docker images
docker rm old_containers
docker rmi old_images

sudo docker run -t -i wesovi/development:latest /bin/bash
docker ps -a
docker start container_id
docker ps




